plugins {
	alias(libs.plugins.convention.plugin.multiplatform.library)
	alias(libs.plugins.convention.plugin.tests)
	alias(libs.plugins.convention.plugin.data.store)
	alias(libs.plugins.kotlinx.serialization)
}

kotlin {
	sourceSets {
	}
}